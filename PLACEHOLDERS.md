# Placeholders

- app/about/page.tsx — the "Our story" milestones timeline uses phase labels (Origins / Fertilizers / Energy / Agriculture / Today), NOT dates — deliberately, to avoid inventing a founding date. Swap in real dated milestones when NTA confirms them.
- app/global-markets/page.tsx — the "Trade corridors" ledger lists representative origin→destination lanes (art-directed, labelled "Representative corridors"). Confirm real lanes with NTA before publishing.
- components/home/HeroDesk.tsx (removed) — the "cargo manifest" panel listed representative product/route corridors (e.g. "UREA · MIDDLE EAST → SOUTH ASIA"), invented for art direction and labeled "representative lines" — confirm with NTA these corridors are ones they actually serve, or supply real ones.
- Bento card images on app/{about,why-nta,sustainability,global-markets}/page.tsx (the `*Images` arrays) and the commodities division photos (lib/data.ts IMG) are illustrative stock, now VENDORED LOCALLY in public/images/vendor/*.jpg (downloaded from Unsplash so the deployed static site has zero external-image dependency — hotlinked Unsplash was failing to load in some environments). Subject-matched per card (Dubai skyline, world-trade map, container port, lab QC, seedling, handshake, crop fields, Gulf energy vessel, rice terraces, etc.). Fine to ship; replace with NTA's own photography of real operations when available.
- Flagship portfolio on the homepage is now components/home/FlagshipCarousel.tsx — an adaptation of the 21st.dev "Feature Carousel" (@0xUrvish, id 10466), re-themed to the trading-desk palette and driven by 8 real flagship grades using their local /fertilizers/*.png spec-sheet posters. The grade blurbs are concise real copy derived from lib/data.ts (grades/crops).
